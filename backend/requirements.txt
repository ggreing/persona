# --- core 서버 & API ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
motor>=3.7  # MongoDB 비동기 드라이버 (PyMongo 4.x 호환)
Jinja2==3.1.4 # HTML 템플릿 렌더링

# --- 벡터 DB ---
qdrant-client==1.9.1

# --- 메시지 큐 ---
pika==1.3.2                     # RabbitMQ

# --- 오브젝트 스토리지 ---
minio==7.2.7

# --- LLM & 임베딩 ---
google-generativeai             # Gemini API SDK
sentence-transformers==2.6.1    # all-MiniLM-L6-v2
torch>=2.0,<3.0                 # CPU/GPU 자동 선택

# --- TTS (대화 피드백용) ---
gTTS==2.5.1

# --- Google Cloud 인증 ---
google-auth==2.28.1             # Google Cloud 인증
google-auth-oauthlib==1.2.0     # OAuth 인증
google-auth-httplib2==0.2.0     # HTTP 클라이언트

# --- HTTP 클라이언트 ---
httpx==0.27.0                   # TTS API 호출용

# --- 편의 ---
python-dotenv==1.0.1            # .env 로드

# --- 옵션: 추적/로깅 분석 ---
pandas==2.2.2  